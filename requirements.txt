# Complete dependency set for the pure-NumPy core: a single
# `pip install -r requirements.txt` is enough to run run_all.py and pytest tests/.
numpy>=1.21
matplotlib>=3.5      # figure rendering across the experiment + figure scripts
pandas>=1.5
scipy>=1.10          # scipy.stats.t for the Appendix-G closed-loop power calculation
scikit-learn>=1.3    # LogisticRegressionCV in run_integration_control.py; PCA in figure scripts
seaborn>=0.12        # KDE contour rendering + colorblind palette in redesign_figure_trajectory.py
pytest>=7.0          # test suite (tests/)
# torch is NOT required for the primary NumPy pipeline above; it is needed only to
# re-run the Appendix-G closed-loop alignment (experiments/run_closed_loop_alignment.py),
# together with the PyTorch companion repo. Install separately: pip install torch
